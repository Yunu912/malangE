import { useState } from 'react';

export default function NewMallangModal({ onClose, onCreate }) {
  const [name, setName] = useState(''); const [category, setCategory] = useState('기타'); const [softness, setSoftness] = useState('3'); const [sound, setSound] = useState(''); const [review, setReview] = useState(''); const [shop, setShop] = useState(''); const [price, setPrice] = useState(''); const [image, setImage] = useState(''); const [imageFile, setImageFile] = useState(null);
  const selectImage = (event) => { const [file] = event.target.files; if (!file) return; setImageFile(file); const reader = new FileReader(); reader.onload = () => setImage(reader.result); reader.readAsDataURL(file); };
  const submit = async (event) => { event.preventDefault(); await onCreate({ name, category, softness, sound, review, shop, price, image: imageFile }); };
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal write-modal" role="dialog" aria-modal="true" aria-label="신규 말랑이 등록" onMouseDown={(event) => event.stopPropagation()}><button className="modal-close" type="button" onClick={onClose}>×</button><p className="eyebrow">NEW MALLANG</p><h2>신규 말랑이 등록</h2><form onSubmit={submit}><label>이름<input value={name} onChange={(e) => setName(e.target.value)} placeholder="예: 딸기 젤리 말랑이" required /></label><label>말랑이 종류<select value={category} onChange={(e) => setCategory(e.target.value)}><option>말랑이</option><option>크런치</option><option>왁뿌볼</option><option>기타</option></select></label><label>말랑도<select value={softness} onChange={(e) => setSoftness(e.target.value)}>{[1, 2, 3, 4, 5].map((value) => <option key={value} value={value}>{'★'.repeat(value)}{'☆'.repeat(5 - value)} ({value})</option>)}</select></label><label>소리<input value={sound} onChange={(e) => setSound(e.target.value)} placeholder="예: 쫀득쫀득" /></label><label>리뷰<textarea value={review} onChange={(e) => setReview(e.target.value)} placeholder="말랑이의 촉감과 특징을 적어 주세요." /></label><label>구매처<input value={shop} onChange={(e) => setShop(e.target.value)} placeholder="예: 말랑이샵 강남점" /></label><label>가격<input type="number" min="0" value={price} onChange={(e) => setPrice(e.target.value)} placeholder="예: 12000" required /></label><label>사진 첨부<input className="file-input" type="file" accept="image/*" onChange={selectImage} /></label>{image && <img className="upload-preview" src={image} alt="신규 말랑이 미리보기" />}<button className="primary full">신규 말랑이 등록</button></form></section></div>;
}
